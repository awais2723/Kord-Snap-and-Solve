/**
 * The code defines a functional component SignpPage that renders the SignupScreen component in a
 * TypeScript React application.
 */
import { SignupScreen } from '@/src/screens';

const SignpPage = () => <SignupScreen />;

export default SignpPage;
